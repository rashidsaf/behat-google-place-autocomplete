#!/bin/bash +x

. "${DIR}"/lib/args.sh

PROJECT_NAME=behatgoogleplaceautocomplete

docker run --rm -it -w /data -v $(pwd):/data --net ${PROJECT_NAME}_default \
    --link web:behatgoogleplaceautocomplete.local \
    php:cli php $ARGS
