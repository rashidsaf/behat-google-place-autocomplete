#!/bin/bash +x

ARGS=''
for i in "$@"; do
  ARGS="$ARGS ${i}"
done

PROJECT_NAME=behatgoogleplaceautocomplete

docker run -it -w /data -v $(pwd):/data --net ${PROJECT_NAME}_default \
    --link web:behatgoogleplaceautocomplete.local \
    php:cli php $ARGS
